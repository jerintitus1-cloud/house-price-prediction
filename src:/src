# main.py (Example of clean production execution)
import os
from src.data_preprocessor import load_and_split_data, build_preprocessor_pipeline
from src.model_trainer import train_production_model, export_model

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
DATA_PATH = os.path.join(BASE_DIR, 'data', 'house_prices.csv')

# 1. Pipeline Processing
X_train, X_test, y_train, y_test = load_and_split_data(DATA_PATH)
preprocessor = build_preprocessor_pipeline()

# 2. Production Engine Build
trained_pipeline = train_production_model(X_train, y_train, preprocessor)

# 3. Secure Export
export_model(trained_pipeline, BASE_DIR)